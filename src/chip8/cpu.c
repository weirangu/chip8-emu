#include <stdio.h>
#include <stdlib.h>
#include "chip8/cpu.h"

// Initializes the CPU with the given file
// Make sure program is open in r mode and in binary
// ie. "rb"
void init_cpu(chip8_cpu* cpu){
}
